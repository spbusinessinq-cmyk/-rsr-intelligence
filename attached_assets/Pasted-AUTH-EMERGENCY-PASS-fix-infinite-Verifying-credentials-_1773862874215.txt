AUTH EMERGENCY PASS — fix infinite “Verifying credentials…” loading state in production now

Do NOT redesign the site.
Do NOT change the visual identity.
Do NOT touch working chat, inbox, command console, Signal Room, Sage, or layout unless required to fix auth boot.

MAIN GOAL
The live production app is still getting stuck forever on:
“Verifying credentials…”

Supabase URL config has already been corrected to the live EdgeOne domain.
So this is now an app-side auth/session boot problem.

I need this fixed immediately and safely.

==================================================
1. AUDIT AUTH BOOT FLOW
==================================================

Inspect the full auth startup path:
- getSession / getUser / getClaims usage
- onAuthStateChange usage
- loading state initialization
- protected route gating
- any effect dependencies that can cause hanging or loops

Find exactly why loading never resolves.

==================================================
2. NO INFINITE LOADER — EVER
==================================================

Implement a hard guarantee:
- auth loading must always resolve
- no infinite spinner / no permanent “Verifying credentials…”

Requirements:
- add a safe timeout fallback
- if auth/session check stalls, exit the loading state cleanly
- if user is not authenticated, route them to login or public state
- if user is authenticated, continue normally

Do NOT leave any path where loading can remain true forever.

==================================================
3. PRODUCTION DOMAIN / SESSION HARDENING
==================================================

Make sure auth boot works correctly on the live EdgeOne domain.

Requirements:
- handle stale tokens / invalid sessions cleanly
- if session refresh fails, sign out locally and exit the loader
- do not trap the user in a half-authenticated state

==================================================
4. PROTECTED ROUTES
==================================================

Investigation Room / Command Console / protected areas must:
- wait for auth briefly
- then either render or redirect
- never freeze forever

==================================================
5. DEBUGGING / VISIBILITY
==================================================

Add temporary console diagnostics so the auth boot path is obvious during testing:
- auth start
- session found / no session
- refresh success / refresh fail
- loader timeout fallback hit
- redirect decision

Do not show raw errors to users, but make debugging visible in console.

==================================================
6. FINAL VERIFICATION
==================================================

Before finishing, explicitly verify:
- live user can enter Investigation Room
- refresh on protected page works
- invalid/stale session exits cleanly
- no endless “Verifying credentials…” state remains
- existing working systems remain intact

SUCCESS CRITERIA

This pass is complete only if:
- “Verifying credentials…” cannot hang forever anymore
- authenticated users enter the app normally
- unauthenticated/stale sessions fail cleanly instead of freezing
- no existing working features are broken